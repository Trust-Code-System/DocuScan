import { toolMetadata } from "@/lib/seo";

export const metadata = toolMetadata("receipt-scanner");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
